export default function InboxPage() {
  return (
    <main className="flex-1 p-8">
      <h1 className="text-2xl font-semibold">Lead Inbox</h1>
      <p className="text-muted-foreground mt-2">
        Unified queue of discovered and inbound leads, sortable by fit,
        credibility, and recency. Wired up once the outbound and inbound
        graphs exist.
      </p>
    </main>
  );
}

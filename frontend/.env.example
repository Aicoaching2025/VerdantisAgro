NEXT_PUBLIC_API_URL=http://localhost:8000

# One tenant today (see lib/config.ts) -- matches the slug used to seed the
# backend's Tenant row.
NEXT_PUBLIC_TENANT_SLUG=verdantis

# Clerk (dashboard auth). No live instance configured yet -- see backend's
# core/auth/clerk.py for the corresponding session-token verification.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
